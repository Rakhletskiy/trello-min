import { addNewCardText, onAddCard, onDelCard, setNewCardText, reorderCards } from './cards-actions';
import { fetchData, onAddCol, onDelCol } from './column-actions';

export {
    addNewCardText,
    onAddCard,
    onDelCard,
    setNewCardText,
    reorderCards,
    fetchData,
    onAddCol,
    onDelCol
}